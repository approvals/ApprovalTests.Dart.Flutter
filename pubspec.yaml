name: approval_tests_flutter
description: Approval Tests implementation in Dart (Flutter). Inspired by ApprovalTests.
version: 1.2.0
repository: https://github.com/approvals/ApprovalTests.Flutter
homepage: https://github.com/approvals/ApprovalTests.Flutter.git
issue_tracker: https://github.com/approvals/ApprovalTests.Flutter/issues
# publish_to: 'none'

topics:
  - test
  - approval
  - approvals
  - tests
  - approval-tests

environment:
  sdk: ">=3.0.0"

dependency_overrides:
  meta: ^1.17.0

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  approval_tests: ^1.3.0
  test: ^1.24.0
  test_api: ^0.7.4
  analyzer: ^7.7.1

dev_dependencies:
  sizzle_lints: ^2.1.7
