name: approval_tests_flutter
description: Approval Tests implementation in Dart (Flutter). Inspired by ApprovalTests.
version: 1.3.0
repository: https://github.com/approvals/ApprovalTests.Dart.Flutter
homepage: https://github.com/approvals/ApprovalTests.Dart.Flutter
issue_tracker: https://github.com/approvals/ApprovalTests.Dart.Flutter/issues
# publish_to: 'none'

topics:
  - test
  - approval
  - approvals
  - tests
  - approval-tests

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  approval_tests: ^1.3.6
  analyzer: ^7.7.1
  match: ^0.4.1

dev_dependencies:
  sizzle_lints: ^2.1.7
